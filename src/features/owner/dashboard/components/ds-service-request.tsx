// ── component ──────────────────────────────────────────────────────────────
const DsServiceRequest = () => {
	return (
		<>
			<p>Section services request</p>
		</>
	);
};

export default DsServiceRequest;
